defmodule RepoAgent.TestWriteRepo do
  def delete(_, _) do
    "delete/2 from TestWriteRepo"
  end

  def insert(_, _) do
    "insert/2 from TestWriteRepo"
  end

  def update(_, _) do
    "update/2 from TestWriteRepo"
  end
end
